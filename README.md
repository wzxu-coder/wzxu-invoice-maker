# WZXU Invoice Maker

Local-first invoice creation without an account. Run `npm install`, then `npm run dev`.

The app stores invoice records in IndexedDB, keeps calculations in a pure domain function, and does not use authentication, Supabase, a server database, or cloud synchronization.
